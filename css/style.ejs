<style>
     :root {
        --bgColor: black;
        --txtColor: white;
        --borderColor: rgb(67, 255, 161);
        --ybubbleColor: rgb(103, 255, 103);
        --obubbleColor: white;
        --ChatBg: rgb(88, 88, 88);
    }
    
    * {
        margin: 0;
        padding: 0;
    }
    
    .mainBody {
        display: flex;
        justify-content: space-between;
        height: 92.3vh;
    }
    
    .contacts {
        color: var(--txtColor);
        padding-top: 10px;
        padding-right: 5px;
        padding-left: 5px;
        text-align: center;
        border-right: 1px solid var(--borderColor);
        width: 30vw;
        background-color: var(--bgColor);
        height: 92.3vh;
        overflow-y: auto;
    }
    
    .userSearch {
        display: flex;
        flex-direction: column;
        height: 150px;
        align-items: center;
        padding-top: 10px;
        padding-bottom: 20px;
        border-bottom: solid 2px white;
    }
    
    .options {
        height: 120px;
        width: 100%;
        overflow: auto;
        margin: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    
    .contact {
        padding-top: 10px;
        padding-bottom: 10px;
        transition-duration: 300ms;
        display: flex;
        justify-content: center;
        cursor: grab;
    }
    
    .contact:hover {
        background-color: white;
        color: black;
    }
    
    .chating {
        width: 70vw;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        background-color: var(--ChatBg);
    }
    
    .chat {
        padding: 10px;
        border-radius: 10px;
        max-width: 50vw;
        margin-bottom: 5px;
        word-wrap: break-word;
        line-break: loose;
    }
    
    .eachSearchUser {
        background-color: rgb(226, 226, 226);
        transition-duration: 0.5s;
        cursor: pointer;
    }
    
    .eachSearchUser:hover {
        background-color: rgb(224, 193, 146);
    }
    
    .chats {
        display: flex;
        flex-direction: column;
        padding-left: 5vw;
        padding-right: 5vw;
        height: 83.3vh;
        overflow-y: auto;
    }
    
    .you {
        align-self: flex-end;
        background-color: var(--ybubbleColor);
    }
    
    .other {
        align-self: flex-start;
        background-color: var(--obubbleColor);
    }
    
    .sending {
        padding-left: 5vw;
        padding-right: 5vw;
        display: flex;
        justify-content: space-between;
        background-color: rgb(236, 215, 188);
        padding-top: 10px;
        padding-bottom: 10px;
        height: 9vh;
    }
    .unread_num {
        margin-left: 5px;
        padding: 2px;
        padding-right: 4px;
        background-color: red;
        border-radius: 100px;
    }

    /* width */
    
     ::-webkit-scrollbar {
        width: 5px;
    }
    /* Track */
    
     ::-webkit-scrollbar-track {
        background: #f1f1f1;
    }
    /* Handle */
    
     ::-webkit-scrollbar-thumb {
        background: #888;
    }
    /* Handle on hover */
    
     ::-webkit-scrollbar-thumb:hover {
        background: #555;
    }
</style>